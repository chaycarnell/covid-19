test('valueOne + valueTwo should return 10', () => {
  // Set some initial values
  const valueOne = 4;
  const valueTwo = 6;
  // Add values together
  const result = valueOne + valueTwo;
  // Observe the expected output
  expect(result).toEqual(10);
});
